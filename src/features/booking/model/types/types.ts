export type Car = {
  id: string
  label: string
}

export type City = {
  id: string
  label: string
}

export type FormValues = {
  carType: string
  rentalPlace: string
  returnPlace: string
  rentalDate: string
  returnDate: string
}